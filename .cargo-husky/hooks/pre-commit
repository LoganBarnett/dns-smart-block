#!/bin/sh
# Pre-commit hook for dns-smart-block
# Automatically formats all Rust code before committing.

set -e

echo "Running cargo fmt --all..."
nix develop --command cargo fmt --all

echo "✓ Code formatted successfully."
